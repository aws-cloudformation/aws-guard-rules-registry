- input:
    Resources:
      daxCluster:
        Type: AWS::DAX::Cluster
        Properties:
          ClusterName: "MyDAXCluster"
          NodeType: "dax.r3.large"
          ReplicationFactor: 1
          IAMRoleARN: "arn:aws:iam::111122223333:role/DaxAccess"
          Description: "DAX cluster created with CloudFormation"
          SubnetGroupName: !Ref subnetGroup
          ClusterEndpointEncryptionType: TLS
          SSESpecification:
            SSEEnabled: true
  expectations:
    rules:
      aws_dax_cluster_encryption_at_rest: PASS

- input:
    Resources:
      daxCluster:
        Type: AWS::DAX::Cluster
        Properties:
          ClusterName: "MyDAXCluster"
          NodeType: "dax.r3.large"
          ReplicationFactor: 1
          IAMRoleARN: "arn:aws:iam::111122223333:role/DaxAccess"
          Description: "DAX cluster created with CloudFormation"
          SubnetGroupName: !Ref subnetGroup
          ClusterEndpointEncryptionType: TLS
          SSESpecification:
            SSEEnabled: false
  expectations:
    rules:
      aws_dax_cluster_encryption_at_rest: FAIL

- input:
    Resources:
      daxCluster:
        Type: AWS::DAX::Cluster
        Properties:
          ClusterName: "MyDAXCluster"
          NodeType: "dax.r3.large"
          ReplicationFactor: 1
          IAMRoleARN: "arn:aws:iam::111122223333:role/DaxAccess"
          Description: "DAX cluster created with CloudFormation"
          SubnetGroupName: !Ref subnetGroup
          ClusterEndpointEncryptionType: TLS
  expectations:
    rules:
      aws_dax_cluster_encryption_at_rest: FAIL